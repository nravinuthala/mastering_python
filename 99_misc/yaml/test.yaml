---
- name: playbook to run config commands
  hosts: csr03
  gather_facts: no

  tasks:
  - name: running config command using ios_config module
    ios_config:
      lines:
        - description test interface
      parents: interface GigabitEthernet1
    register: output

  - name: display output
    debug:
      var: output